/**
 * Flat translation dictionary loaded from each language's `file_url`.
 * Keys are numeric IDs (as strings); values are the translated strings.
 *
 * Example: { "12": "Hello", "1": "login to MetaRGB" }
 */
export type Translations = Record<string, string>;
